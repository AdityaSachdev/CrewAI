[project]
name = "analyst"
version = "0.1.0"
description = "analyst using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.9.3",
    # macOS 12 (x86_64) needs an ORT build that still ships compatible wheels.
    "onnxruntime<1.20; sys_platform == 'darwin' and platform_machine == 'x86_64'",
]
[project.scripts]
analyst = "analyst.main:run"
run_crew = "analyst.main:run"
train = "analyst.main:train"
replay = "analyst.main:replay"
test = "analyst.main:test"
run_with_trigger = "analyst.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
# Force uv to resolve packages for macOS Intel (this machine),
# avoiding versions that only publish non-compatible wheels.
required-environments = [
    "sys_platform == 'darwin' and platform_machine == 'x86_64'",
]